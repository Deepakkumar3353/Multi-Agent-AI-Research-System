# Core LangChain ecosystem
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0

# LCEL / Runnables support (comes via core but keep explicit)
langchain-experimental>=0.0.60

# Google Gemini (Generative AI)
langchain-google-genai>=1.0.0
google-generativeai>=0.7.0

# Tavily Search Tool
tavily-python>=0.3.3

# Web scraping
beautifulsoup4>=4.12.3
requests>=2.31.0
lxml>=5.2.1

# Async support (important for agents/tools)
httpx>=0.27.0
aiohttp>=3.9.5

# Environment & config
python-dotenv>=1.0.1
pydantic>=2.7.0

# Logging & utilities
loguru>=0.7.2

# Optional: parsing / text processing
tiktoken>=0.7.0

# Optional: JSON handling improvements
orjson>=3.10.0

# Optional: retry & robustness
tenacity>=8.2.3

# Optional: CLI / dev tools
rich>=13.7.1

# If you plan to expose via API (recommended)
fastapi>=0.111.0
uvicorn>=0.30.0

# If you plan to test
pytest>=8.2.0

# If you plan to cache or store results
redis>=5.0.4

streamlit